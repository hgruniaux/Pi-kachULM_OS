#pragma once

/*
      Memory Allocator
*/
/*
class MemAlloc {
 public:
  // Constructor
  MemAlloc(PageAlloc ancestor);
  // Utility
  void* malloc(size_t nb_oct, uint64_t align);
  void free(void* memory_allocated);

 private:
  PageAlloc m_ancestor;
};
*/
